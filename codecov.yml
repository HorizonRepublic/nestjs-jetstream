coverage:
  status:
    project:
      default:
        target: 70%
        threshold: 5%
    patch:
      default:
        target: 80%

ignore:
  - "src/server/infrastructure/**"
  - "src/server/strategy.ts"
